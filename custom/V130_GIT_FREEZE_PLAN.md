# v1.30 Git 冻结计划

## 当前状态

- 分支：`master`。
- 基线 HEAD：`9a4b0197a03936a519b1ee15bd2d472e9870e995`。
- 暂存区：0。
- 本轮未提交、推送、打标签或部署。

## 推荐策略

由于 rc.4 未形成独立提交，而 v1.30 与此前工作树修改广泛重叠，推荐在 P1 阻断项解决并获得明确授权后，按完整工作树一次性冻结。不要猜测拆分 rc.4，也不要改写历史。

建议流程（仅计划，不在本轮执行）：

1. 解决 P1，重新构建并验证 v1.30 生产包。
2. 复跑全部检查，确认 staged=0 后由发布人审阅完整 diff。
3. 明确授权后统一暂存并提交：`release: freeze 柚喜饰界 v1.30 release candidate`。
4. 建议标签：`v1.30.0-rc.1`。
5. 再次明确托管目标和自动部署行为后才允许推送/部署。

回滚锚点为上述基线 HEAD；禁止使用破坏性重置覆盖未保存工作。
