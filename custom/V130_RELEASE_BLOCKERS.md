# v1.30 发布阻断清单

## P0

无。当前本地候选未发现数据丢失、页面不可达、严重语义错误或基础回归失败。

## P1：正式发布前必须解决

1. 决定托管平台，并处理 `master` 推送会自动触发 GitHub Pages 的工作流风险。
2. 配置根域到 `www` 的跳转、HTTPS/TLS 和 HTTP 到 HTTPS。
3. 验证生产服务器真实 404。
4. 按最终人工清单完成生产切换后的 Smoke Test。

已本地解决：运营主体、ICP备案、二维码人工确认、最终发布授权和 v1.30 发布包均已闭环。发布包已从完整 working tree 重建到 `release/v1.30-rc.1/`，151 个文件通过独立包、HTTP、SEO、链接、资源和真实 404 检查。旧 `_site` v1.23 rc.3 包仍不得使用。

## P2

- 在生产网络做 Core Web Vitals、缓存和压缩测试。
- 拆分/去重约 212 KB 的公共 CSS。
- 补做屏幕阅读器与完整键盘人工验收。

## P3

- 4 个品牌专属图片缺口已在迁移台账中授权保留；获得合法素材后再补，不应临时下载或伪造。
