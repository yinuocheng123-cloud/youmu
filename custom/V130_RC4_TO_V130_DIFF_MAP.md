# v1.23 rc.4 至 v1.30 差异映射

## 可追溯性结论

仓库 HEAD 仍是 `v1.23.0-rc.3` 对应基线；rc.4 没有独立提交或标签，仅存在于后续工作树演进中。因此无法在不猜测、不重写历史的前提下精确重建 rc.4 边界。

## 安全分类

- 可明确归属 v1.30：`migrate-v130-*`、`check-v130-*`、`v130-*` 台账/审计文件和 v1.30 阶段截图。
- 与 rc.4 重叠：127 个 HTML、`styles.css`、`data/site-content.js` 及原有检查脚本；这些文件包含连续累积修改。
- 生成证据：迁移 JSON/CSV/Markdown、截图和本轮收口报告。
- 可明确归属 rc.4 且不与 v1.30 重叠：无可靠文件级证据。

## 冻结建议

不要按 rc.4/v1.30 猜测拆分，也不要重构历史。正式授权后将当前完整工作树作为一个 v1.30 候选整体提交最安全。建议提交信息：`release: freeze 柚喜饰界 v1.30 release candidate`；建议候选标签：`v1.30.0-rc.1`。本文件不构成提交、标签或部署授权。
